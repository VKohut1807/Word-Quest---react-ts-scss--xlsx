const baseUrl = "";

export const ROUTES = {
    HOME: baseUrl + "/",
    DICTIONARY: baseUrl + "/dictionary",
    SETTINGS: baseUrl + "/settings/",
    GAMES: {
        ROOT: baseUrl + "/games",
        TWIN_QUEST: baseUrl + "/games/twin-quest",
    },
};
